# 公开前核对

GitHub `lunaship/dsh-links` = **本插件仓 only**。

同级本机目录（勿推进本 remote）：

- `../dsh-links-app` — 私有 App
- `../dsh-links-relay` — 中继占位

确认公开前：本仓 `git ls-files` 不得出现 Android/`app/src`。回复「确认公开」后再改可见性。
