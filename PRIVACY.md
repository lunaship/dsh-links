# Privacy

DSH Links Beta 的数据与权限说明。这不是 Google Play 数据安全表；它说明这款高权限 App 在手机上做什么。

## 我们不收集什么

- App 不建立项目账号。
- App 不含遥测、广告和远程崩溃上报。
- 局域网路径下，会话内容只在你的主机和你的手机之间传输。
- DSH Links Relay 目前是维护者内测：流量由 Relay 实时转发，不持久化会话内容、消息、Prompt、文件、工作区、审批内容或设备内容。接入码只由维护者发放，不会写进本仓库或安装包。
- Relay 数据面只在内存中维护在线连接、挑战、流和限流状态；连接结束或 Relay 重启后，这些临时状态会消失。
- Relay 的 Control 控制面会持久化完成鉴权和运维所需的最小元数据，包括 Host/Route/设备标识、公钥、凭证哈希及有效期、撤销状态、在线时间、聚合流量与连接统计，以及控制台审计事件。Control 不保存会话正文。
- Relay 终止外层 TLS，但不会解密 App 与插件之间的内层 TLS；因此 Relay 运营者可能看到连接和流量的外层元数据，却不能从 Relay 取得 Token、Prompt、文件或响应正文。

## 手机本地存储

以下数据保存在 Android 本地：

- 主机地址与显示名称
- 设备名称
- 证书 SHA-256 指纹
- 配对 Token（使用 Android Keystore 派生密钥加密）
- 主题、语言等本地设置

Token 与 HostStore 被排除在云备份和设备迁移之外（`allowBackup=false`，并提供 `fullBackupContent` / `dataExtractionRules`）。

## 会话内容走哪里

局域网路径下，会话、消息、工具调用和审批请求由手机直接向你自己的 DSH 主机请求（`18640`）。内容停留在你的主机和你的手机上。

内测 Relay 路径下，同样的请求经 Relay 实时转发到已接入的电脑插件，再到达本机 `18640`。Relay 不保存这些内容。

## 权限用途

| 权限 | 用途 |
|---|---|
| 相机 | 扫描配对二维码。可拒绝，改为手动输入地址和配对码。没有摄像头的设备仍可安装。 |
| 通知（Android 13+） | 当前会话 SSE 仍可达时，用于审批、任务完成、会话停止的本地提醒。可拒绝，工作台仍可用。这不是进程被杀后的独立推送；全会话后台通知渠道待定，尚未启用。 |
| 麦克风 | 系统语音识别输入。可拒绝。 |
| 网络 | 连接本机 DSH 插件。 |
| 图片 | 通过系统文档选择器授权单个内容 URI 发送图片，不申请全局存储权限。 |

## 删除数据

- 清除 App 数据或卸载 App 会删除手机本地主机列表、Token 和设置。
- 这不会自动吊销电脑端的设备记录。丢失手机或卸载后，仍应在 DSH「手机连接」面板吊销该设备。
